{
  "name": "login",
  "version": "0.0.0",
  "description": "login server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.13.0"
  }
}
